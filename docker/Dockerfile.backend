# Backend Dockerfile - Express API
FROM node:20-alpine

WORKDIR /app

COPY backend/package*.json ./
RUN npm install --omit=dev

COPY backend/ ./

ENV PORT=5000
EXPOSE 5000

# Heroku/Railway inject $PORT at runtime; the app already reads process.env.PORT
CMD ["node", "src/server.js"]
